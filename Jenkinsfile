pipeline {
  agent any
  
  stages {
    stage('CLEANUP Workspace')
      steps {
        cleanWs()

     stage('CLEANUP Workspace') {
       steps {
         sh 'git clone https://github.com/aissatou00/depotgithub.git'
             }
           }
        }
  }

